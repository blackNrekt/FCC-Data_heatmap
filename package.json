{ "homepage": "https://blacknrekt.github.io/FCC-Data_heatmap/index.html",
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"  
  }
}
